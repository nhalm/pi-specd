pre-commit:
  parallel: true
  commands:
    lint-and-fmt:
      run: make lint fmt

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-push:
  commands:
    check:
      run: make check
